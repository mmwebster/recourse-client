// @desc Standalone text property editor, pass it a property and the owner and it handles
//       the rest.
// @param attr The attribute to edit
import Ember from 'ember';

export default Ember.Component.extend({
});
