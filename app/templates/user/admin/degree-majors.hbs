{{#list-standard items=model as |item|}}
  <div class="layout-column layout-align-space-around-start">
    {{#paper-button}}<h4>{{item.title}}</h4>{{/paper-button}}
    <div class="tree">
      {{node-editor node=item.tree parentRecurrenceDepth=0}}
    </div>
  </div>
{{/list-standard}}
